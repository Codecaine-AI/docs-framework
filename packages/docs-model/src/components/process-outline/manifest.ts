"use client";

import type { ComponentManifest } from "../types";

export const manifest: ComponentManifest = {
  name: "process-outline",
  ownedTypes: ["process-outline"],
  description:
    "Process outline: an ordered recursive step tree with nested substeps and leaf clarification notes, rendered as a connected hierarchy on a rail. Process-outline notation is its import and projection format. Use it to explain how a process decomposes from phases into substeps; canvas covers spatial relationships and sequence covers exact exchanges.",
};
